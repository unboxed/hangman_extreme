<%= form_for(@redeem_winning) do |f| %>
    <%= link_to 'cancel', redeem_winnings_path, id: 'cancel' %> Redeem<br/>
    You about to redeem
    <b><%= @redeem_winning.prize_amount %> prize points</b>
    for
    <i>
    <% if @redeem_winning.prize_type.include?('airtime') %>
        R5
        <%= "vodago" if @redeem_winning.prize_type.include?('vodago') %>
        <%= "mtn" if @redeem_winning.prize_type.include?('mtn') %>
        <%= "cell c" if @redeem_winning.prize_type.include?('cell') %>
        airtime
    <% elsif @redeem_winning.prize_type.include?('mxit_money') %>
        <%= number_to_currency(@redeem_winning.prize_amount.to_f / 100, :unit => "R") %> mxit money
    <% else %>
        <%= @redeem_winning.prize_amount %> <%= "clue points" %>
    <% end %>
    </i>
    <br/>
    <%= f.hidden_field :prize_type %>
    <%= f.hidden_field :prize_amount %>
    <%= f.hidden_field :state %>
    <%= f.submit 'redeem', id: 'submit' %>
<% end %>
