<br/>
<% ['daily','weekly','monthly'].each do |time_period| %>
    <%= link_to_unless params[:period] == time_period,
                       time_period,
                       users_path(period: time_period, rank_by: params[:rank_by]),
                       id: "#{time_period}_#{params[:rank_by] || 'rating'}" %> |
<% end %>
<br/>
<% ['points','rating','precision'].each do |rank| %>
    <%= link_to_unless params[:rank_by] == rank,
                       rank,
                       users_path(period: params[:period], rank_by: rank),
                       id: "#{params[:period] || 'daily'}_#{rank}" %> |
<% end %>
<br/>
<%= link_to 'scoring', explain_path(action: 'scoring_categories'), id: 'scoring_categories' %>  explained |
view <%= link_to 'winners', winners_path(period: params[:period], reason: params[:rank_by]), id: 'winners' %>