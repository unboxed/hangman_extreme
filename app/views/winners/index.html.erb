<b>Yesterday's <i><%= params[:reason] %></i> Winners</b><br/>
<% if @winners.empty? %>
    <p><b>Coming Soon</b></p>
<% else %>
<table>
  <tbody>
  <tr>
      <td><b>name</b> | </td>
      <td><b>moola</b> | </td>
  </tr>
  <%= content_tag_for(:tr, @winners) do |winner| %>
      <td><%= display_user_name(winner.name) %> | </td>
      <td><%= winner.amount %> | </td>
  <% end %>
  </tbody>
</table>
<% end %>


<%= link_to 'home', root_path, id: 'root_page' %> |
<%= link_to_unless params[:reason] == 'rating',
                   'rating',
                   {reason: 'rating'} %> |
<%= link_to_unless params[:reason] == 'wins',
                   'wins',
                   {reason: 'wins'} %> |
<%= link_to_unless params[:reason] == 'precision',
                   'precision',
                   {reason: 'precision'} %>
